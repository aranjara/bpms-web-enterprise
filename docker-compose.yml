services:
web:
build: .
container_name: bpms-web-enterprise
ports:
- "5000:5000"
environment:
- DB_PATH=/app/bpms_web.db
- REPORT_PATH=/app/Informe BPMS.xlsx
- LOG_PATH=/app/error.log
restart: unless-stopped
