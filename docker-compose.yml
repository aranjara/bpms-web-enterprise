services:
  web:
      build: .
          container_name: bpms-web-enterprise
              ports:
                    - '5000:5000'
                        volumes:
                              - .:/app
                                  environment:
                                        - DB_PATH=/app/bpms_web.db
                                              - REPORT_PATH=/app/Reporte BPMS.xlsx
                                                    - LOG_PATH=/app/error.log
                                                          - UPLOAD_FOLDER=/app/uploads
                                                              restart: unless-stopped
                                                              
